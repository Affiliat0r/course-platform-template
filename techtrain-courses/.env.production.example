# Production Environment Configuration
# Copy this file to .env.production and fill in your production credentials
# WARNING: Never commit .env.production to git!

# ============================================================
# SUPABASE PRODUCTION CONFIGURATION
# ============================================================

# Get these from Supabase dashboard: https://app.supabase.com
# Project: techtrain-production
# Location: Project Settings > API

# Supabase URL - Project Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL=https://your-production-project.supabase.co

# Supabase Anon Key - Project Settings > API > Project API keys > anon public
# This is safe to expose in client-side code
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-anon-key-here

# Supabase Service Role Key - Project Settings > API > Project API keys > service_role
# ⚠️ WARNING: NEVER expose this in client-side code or commit to git!
# Only use in server-side code, API routes, and migration scripts
# Store this securely in your password manager
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.your-service-role-key-here

# ============================================================
# APPLICATION CONFIGURATION
# ============================================================

# Production domain (update this with your actual domain)
NEXT_PUBLIC_APP_URL=https://techtrain.nl

# ============================================================
# STRIPE CONFIGURATION (Payment Processing)
# ============================================================

# Get these from Stripe dashboard: https://dashboard.stripe.com/apikeys
# Use LIVE mode keys for production (starts with pk_live_ and sk_live_)

# Stripe Publishable Key (safe to expose in client-side code)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_live_your-publishable-key-here

# Stripe Secret Key (⚠️ KEEP SECRET - server-side only)
STRIPE_SECRET_KEY=sk_live_your-secret-key-here

# Stripe Webhook Secret (⚠️ KEEP SECRET)
# Get this after creating webhook endpoint in Stripe dashboard
# Webhook URL: https://techtrain.nl/api/webhooks/stripe
STRIPE_WEBHOOK_SECRET=whsec_your-webhook-secret-here

# ============================================================
# EMAIL SERVICE CONFIGURATION (Optional)
# ============================================================

# If using custom SMTP instead of Supabase email:

# Resend API Key (recommended for transactional emails)
# RESEND_API_KEY=re_your-resend-api-key-here

# Or SendGrid API Key
# SENDGRID_API_KEY=SG.your-sendgrid-api-key-here

# ============================================================
# MONITORING & ANALYTICS (Optional)
# ============================================================

# Sentry DSN (error tracking)
# NEXT_PUBLIC_SENTRY_DSN=https://your-sentry-dsn-here

# Google Analytics 4 Measurement ID
# NEXT_PUBLIC_GA_MEASUREMENT_ID=G-XXXXXXXXXX

# Vercel Analytics (automatically enabled on Vercel)
# No configuration needed

# ============================================================
# SECURITY CONFIGURATION
# ============================================================

# NextAuth Secret (if using NextAuth.js instead of Supabase Auth)
# Generate with: openssl rand -base64 32
# NEXTAUTH_SECRET=your-nextauth-secret-here

# CORS Allowed Origins (comma-separated)
# ALLOWED_ORIGINS=https://techtrain.nl,https://www.techtrain.nl

# ============================================================
# DEPLOYMENT NOTES
# ============================================================

# For Vercel deployment:
# 1. Add all environment variables in Vercel dashboard:
#    - Go to Project Settings > Environment Variables
#    - Add each variable individually
#    - Mark sensitive variables (service keys, secrets) as "Sensitive"
# 2. Deploy from GitHub repository
# 3. Vercel will automatically use these environment variables

# For manual VPS deployment:
# 1. Copy this file to .env.production on your server
# 2. Fill in all values
# 3. Ensure .env.production is readable only by your app user:
#    chmod 600 .env.production
# 4. Load variables before starting app:
#    source .env.production && npm start

# ============================================================
# SECURITY CHECKLIST
# ============================================================

# Before going to production:
# [ ] All secrets are stored in password manager
# [ ] .env.production is in .gitignore
# [ ] Service role keys are marked as sensitive in Vercel
# [ ] MFA enabled on Supabase account
# [ ] MFA enabled on Stripe account
# [ ] SSL certificate installed and working
# [ ] All API keys rotated from any exposed in documentation
# [ ] Database backups configured
# [ ] Monitoring and alerts configured
